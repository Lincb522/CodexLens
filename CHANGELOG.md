# Changelog

## 2.1.0 — 2026-08-26

### Added

- 当前请求、当前轮次和任务累计三种 Token 口径。
- 多任务实时监控与任务切换。
- 账号累计 Token、额度窗口和基于真实样本的剩余时间预测。
- OAuth、Codex Home、Token、JSON/JSONL、Sub2、CPA 与 Cockpit 账号导入。
- “仅监控”和“登录到 Codex”两种明确的账号切换方式。
- Tibo 公开重置信号周期页。
- 简体中文、繁体中文、英语、日语、韩语、西班牙语和法语。
- 浅色、深色和跟随系统主题。

### Changed

- 概览重组为单一实时上下文主数、三项请求明细和任务/额度/账号动态区。
- Token 详情改为独立标签/数值行，大型精确计数不再挤在三列中。
- 所有精确计数显示 Token 单位，API 费率显示 `/ 1M Token`。
- 本地账本、控制中心和开发者页面统一为 340×705pt 主页面。
- 展开详情改为窗口内浮层，展开和收回不再改变原生菜单尺寸。
- 应用图标与首页品牌标记改为独立的栅格资源。

### Accuracy

- 费用按单次请求应用超长上下文倍率。
- 缓存输入已经包含在输入 Token 中，不重复相加。
- 历史记录缺少完整输入/输出拆分时不生成费用结果。
- 账号累计 Token 直接使用 Codex 本机服务返回值。
