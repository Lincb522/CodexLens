# 界面规范

[← README](README.md) · [文档](docs/README.md)

---

## 窗口

| 项目 | 数值 |
| --- | ---: |
| 菜单宽度 | 340pt |
| 页面高度 | 680pt |
| 页面左右安全边距 | 16–20pt |
| 最小可见字号 | 12pt |
| 数据字体 | 系统字体、等宽数字 |

窗口采用透明 `NSPanel`，由铺满窗口的 `NSVisualEffectView` 使用系统菜单材质（`menu` / `behindWindow`）。SwiftUI 和原生内容容器均不绘制实色背景，内容开启 `allowsVibrancy`；窗口不叠加调色蒙层或顶部色带。浅深色模式跟随设置，材质遵循系统辅助功能选项；蓝色用于进度、选中状态和可操作入口。

仅任务选择、可点击摘要与明细计数使用细描边分组。正文与数字不叠加阴影、发光或彩色背景。

设置中的“Token 数值简写”统一控制菜单栏、首页、记录、明细与热力格悬停提示。默认关闭，显示带分组符的完整整数；开启后使用 K / M / B / T 简写。金额、百分比和实际统计数值不受影响，选择在重启后保留。长数值沿用原有的单位换行与横向巡览，不缩小字号。

## 首页

从上到下：

1. Codex Lens、账号套餐、刷新
2. 当前任务选择
3. 本次上下文、占用率与容量刻度
4. 其中缓存、本次输出、对话明细入口
5. 使用记录
6. 账号额度、Tibo 重置预测

本次上下文取最近一次请求的输入 Token，不显示任务累计。容量刻度标出上限；缓存是输入的一部分，不重复相加。

首页使用记录默认显示本月 1 日至今天，按账号日统计的 UTC 日期对齐，格内不显示日期，悬停可查看当天日期与用量。范围选择支持本月、近 7 天、近 30 天和自选起止日期，在原图表区域内编辑，不改变窗口高度。保留原来的 14.5pt 小格子、2.25pt 间隔和至少 16 周的完整网格，不按选中天数缩放格子或缩窄图表。日期筛选只决定哪些格子显示真实用量；范围外为淡色占位，不表示零用量，也不可点击。超过 16 周的区间可在图表内横向滚动。使用蓝灰透色和整面的轻微斜向反光，边界清晰，不使用中心亮点、外光晕或立体阴影。明暗以全年区间内单日峰值的 25%、50%、75% 分档，不改动用量数值；低档蓝灰，最高档为冰蓝亮面。深色格面使用滤色叠加，保留背景色差；无用量日保留淡灰格面。点击日期进入对应日期的使用记录页，标题入口打开完整记录；首页筛选不影响该页的全年分页、单日和月度汇总。月度列表在剩余内容区内滚动，与固定底部导航保留 12pt 间距。

首页账号额度只显示当前主额度的剩余进度，点击后查看周期、积分和金额。Tibo 使用独立入口，不混入账号周期；账号同步失败也保留公开信号。

## 对话明细

在固定窗口内进入独立页面，底部导航位置不变，不重建或重新测量窗口。屏幕可用高度不足 680pt 时，原生纵向滚动容器保证所有内容仍可到达。

- 按本次请求、当前轮次、任务累计切换。
- 总计、输入、缓存、输出均带 Token 单位，显示方式跟随全局设置。
- 输入箭头向下，输出箭头向上。
- 本轮和任务的 API 估算单列。
- 模型入口提供费率、运行窗口与计数说明。

不使用缩小字体或省略号挤压数据；计数与单位放不下时，单位独占下一行。长任务名和标签使用横向巡览，辅助功能保留完整文本。巡览文字按实际行高裁切，页标题、数字和不同语言的字形保持完整。

## 账号额度

账号周期、积分、API 等价额度、模型专属额度分区排列，不叠放多层卡片。

- 账号有多个周期时，通过周期选择查看，不能把周额度与 5 小时额度合并。
- API 等价额度区分别显示当前可用、周满额、月均满额；信息入口保留估算区间与参考资料。
- 代码补全等模型专属额度单独分组；多个分组通过选择器查看。

## 其他页面

- 页头显示返回与标题，底部导航固定。
- 本地账本的任务名独占一行，项目与累计用量在下一行。
- 设置使用原生分段选择与开关，保留账号、实时追踪和数据管理。
- Tibo 页区分预测与判断依据，保留已确认重置、推文和回复的原帖入口。
- 关于页提供版本、构建号、协议和更新入口；更新页展示版本日志。

## 图标与交互

应用图标和品牌标记保持不变。功能图标统一采用圆润实心轮廓与镂空细节，使用 imagegen 逐个生成的 31 个 PNG 图标，覆盖导航、数据、账号、设置和操作。透明模板资源随正文、选中与禁用状态着色，不自带底板。

原图、提示词、语义清单和浅深色预览位于 `Design/IconSystem/`。运行 `python3 Design/IconSystem/build_png_icons.py`，从原始位图提取透明轮廓、居中并打包 24/48/72px 资源，同时检查尺寸、透明边距与中心偏差。

页面和分段即时切换，不向整扇菜单传播布局动画。按钮反馈不改变几何尺寸。开启“减少动态效果”时关闭位移动画，保留状态本身的可读性。

## 验证范围

界面改动检查浅深色、固定窗口边界、长文本、大数值、七种语言、加载、空状态和错误状态。原生桌面毛玻璃、键盘、VoiceOver、减少动态效果及减少透明度需要运行时检查，不能以静态截图代替。
