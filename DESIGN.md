# 界面规范

[← README](README.md) · [文档](docs/README.md)

---

## 尺寸

| 项目 | 数值 |
| --- | ---: |
| 菜单宽度 | 340pt |
| 主页面高度 | 740pt |
| 页面左右安全边距 | 16pt |
| 最小可见字号 | 12pt |
| 数据字体 | SF Mono |
| 界面字体 | 系统字体 |

所有页面使用相同尺寸和 16pt 左右安全边距。Token 详情在首页内部覆盖展开，不触发 `NSMenu` 重新测量。

## 首页

首页分为四层：

1. 品牌、账号、刷新
2. 运行任务选择
3. 当前上下文
4. 账号使用记录、额度与 Tibo 信号

上下文区域只保留一个大号数值，默认显示最近一次请求的输入 Token。下方三项依次为输入、缓存输入和输出。任务累计通过单独切换查看。

账号使用记录使用近一年日历热力图，颜色深浅只表示当天 Token 用量层级。点击图表进入详情页，查看选中日期、近 30 天与最近九个月汇总。额度和 Tibo 信号保持独立入口，不与任务 Token 混合。

## 颜色与材质

- 整扇菜单的背景由 AppKit 原生 `NSMenu` 材质提供，SwiftUI 根视图保持透明；不在顶部或底部叠加不透明色带。
- 当前上下文使用一块半透明深蓝焦点面板，其余信息使用低透明度连续分组，让桌面材质贯穿整扇窗口。
- 浅色模式使用蓝灰文字；深色模式使用高对比度浅色文字。
- 绿色只表示有效、在线或健康状态。
- 黄色和红色只用于警告与错误。
- 分组不使用装饰性阴影、彩色描边或多层发光，也不在同一区域嵌套多层卡片。

## 文字与数字

- 标签和数值必须有明确的上下关系，不能把多个大数挤在同一行。
- 精确计数显示 `Token` 单位；费率显示 `/ 1M Token`。
- 文本不使用 `minimumScaleFactor`，不换行，不用省略号替代关键标签。
- 长任务名使用停顿式横向巡览。

## 图标

应用图标、品牌标记和内部功能图标均使用 PNG Image Assets。首页品牌标记使用单色透明版本，状态栏使用与当前指标对应的图标。

## 动画

- 数字变化使用内容过渡。
- 页面与分段切换只使用局部淡入淡出。
- 进度条和按钮反馈使用短时长缓动。
- 展开详情只移动详情层和箭头，不改变菜单布局。
- 开启“减少动态效果”时取消重复和位移动画。

## 检查状态

界面改动至少检查：

- 浅色与深色
- 详情展开与收起
- 多任务切换
- 长任务名
- 大 Token 数值
- 七种语言
- “减少动态效果”
