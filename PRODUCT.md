# 产品约束

[← README](README.md) · [文档](docs/README.md)

---

## 范围

- macOS 菜单栏应用
- 当前任务上下文、累计用量、账号额度、参考成本
- 无独立主窗口

## 首页信息顺序

1. 当前账号与刷新状态
2. 运行中的任务
3. 当前任务最近一次请求的上下文输入
4. 本次请求的输入、缓存输入与输出
5. 任务、额度、账号三个分页区域
6. Tibo 公共重置信号

当前上下文是首页主数。任务累计和账号累计不能替代它，也不能使用相同标签。

## 数据规则

- Codex 原始计数、服务端数据、确定性计算和费用估算必须分开标注。
- 缺少字段时显示不可用，不补零，不猜测输入输出比例。
- 账号累计 Token 只使用 `app-server` 返回值。
- 本地历史只统计扫描到的会话，不归属到后来切换的账号。
- 缓存输入已包含在输入 Token 中，合计时不能重复相加。
- 超长上下文费率按单次请求判断，不能按任务总量判断。
- 未定价模型或不完整记录不得进入聚合费用。
- 额度剩余时间只使用同一账号、同一窗口的真实历史样本。

## 账号规则

- “仅监控”不改变 Codex 当前登录。
- “登录到 Codex”必须由用户明确选择。
- 替换默认 `auth.json` 前先备份，再原子写入。
- 凭据不得出现在日志、导出、测试夹具或界面诊断信息中。

## 界面规则

- 菜单宽度为 340pt；首页、本地账本和控制中心高度为 705pt。
- 主页面不使用滚动容器；列表通过分页访问。
- 可见字号不小于 12pt。
- 标签保持单行，不缩小字体；长任务名使用横向巡览。
- 展开 Token 详情不能改变菜单尺寸。
- 浅色、深色和跟随系统使用同一信息结构。
- 所有界面文字进入 String Catalog，并覆盖七种受支持语言。

## Tibo 信号

Tibo 信号是独立的公共事件，不是账号的 5 小时或每周额度重置。首页只显示当前状态和时间；完整页面显示最近确认、当前信号以及预测或七天基线。规则版本、哈希和动态 ID 不进入默认界面。
