# Product

## Register

product

## Users

使用 Codex Desktop 或 CLI 的 macOS 开发者。他们不希望打开网页后台，只想从顶栏确认真实任务标题、实时上下文输入、本地对话总 Token、当前账号官方累计 Token、额度窗口和基于真实消耗历史的剩余时间。

## Product Purpose

把 Codex 上游 Token 计数、真实任务名称和当前账号官方数据变成可靠、低打扰、随时可见的顶栏读数。成功标准是：实测、官方、确定性派生和估算四类证据可以一眼区分；多次模型调用不丢失、不重复；账号总 Token 直接来自服务端；余量预测有样本数和置信度；未知值显示不可用而不是伪造精确数字。

## Brand Personality

鲜活、可信、敏捷。它保留顶栏工具的效率，但采用“实时主数 + 每日动态”的轻量产品界面，而不是 macOS 原生设置面板质感。

## Anti-references

禁止 macOS 原生设置面板质感、大号仪表盘塞进小弹窗、层层圆角卡片、紫色 AI 渐变、无止境加载动画，以及显示“已同步”但实际上仍在扫描的虚假状态。允许一个蓝青色上下文主视觉；其他区域必须回到克制的自适应毛玻璃中。整个弹窗仍使用一层真实透明的系统菜单毛玻璃背景。

## Design Principles

1. 先显示可信状态，再显示漂亮图表。
2. 顶栏面板是快速检查工具，不是缩小后的桌面仪表盘。
3. 当前上下文占用是第一视觉，服务端剩余额度是第二视觉；账号只是上下文，设置和导出退居二级入口。
4. 同步必须快速、可观察、可恢复；失败原因必须可见。账号“只监控”和“登录 Codex”必须是两个明确动作，不能暗中改变默认凭据。
5. 最新调用上下文、本轮 Token、任务累计、账号官方 Token、账号额度、账号积分、本地账本和费率折算必须明确区分。
6. 概览、本地账本和控制中心使用一致的 340×705pt 主页面；内容必须填满可用区域，不能用弹性间距制造大块空白。
7. 顶栏面板禁止滚动容器；长任务列表使用显式分页，Token 详情以窗口内浮层展开且不得改变原生菜单尺寸。
8. 公开 1.05M 模型容量、Codex runtime 窗口和 272K API 超长计费阈值必须分开命名和显示。
9. OAuth、Codex Home、文件导入和直接 Token 登录是独立添加入口；直接粘贴必须兼容原始 Token、API Key、通用 JSON/JSONL、Sub2、CPA 与 Cockpit，并在写入前让用户明确选择“只监控”或“登录到 Codex”。凭据只允许进入权限隔离的本地目录，切换默认登录前必须生成可回滚备份。
10. 不存在公开固定映射时，禁止用 Token 换算 ChatGPT 积分或订阅额度。
11. 任何聚合费用只要包含未定价模型或不完整 Token 拆分，整体必须显示 unavailable，不得静默忽略。
12. 所有用户可见标签、任务来源、额度名称和错误状态都必须经过 String Catalog i18n。
13. 概览不砍数据，但必须分层：实时上下文是唯一大号主数；输入/缓存/输出紧随其后；任务、额度和账号共用一个明确的分段动态区。模型、来源、runtime 窗口、调用次数和完整费率证据收进实时主视觉的一键详情，不依赖悬停，也不删除功能。
14. Tibo 公共重置信号是独立于账号额度的公开重置周期，不是当前账号的 5 小时或每周周期重置。它必须按照“最近确认 → 确认时间加七天的基线 → 新结构化预测临时覆盖 → 确认后清除预测并重新锚定”的状态机工作；概览显示当前阶段和下一时间，详情使用最近确认、当前信号、预测/基线三个节点，预测帖与确认帖都可分别打开。规则关键词不得虚构时间，规则版本、来源状态、哈希和动态 ID 不进入默认界面。

## Accessibility & Inclusion

使用高对比度石墨灰主题和完整的辅助功能语义；兼容键盘焦点与“减少动态效果”，核心状态不能只依赖颜色表达。所有可见界面文字不得小于 12pt，并以自动化源码测试锁定该下限。所有界面标签保持原字号单行完整显示：不缩小、不换行；长任务名称在固定窄宽内使用停顿式横向巡览。
